# Express Server Setup - Completed by BLACKBOXAI

✅ 1. Created package.json (Express + nodemon)  
✅ 2. Created server.js with exact root route `app.get("/", ...)` + static serving  

**Next steps:**  
- `cd portal/portfolio-website`  
- `npm install` (installs Express/nodemon)  
- `npm start` (starts server on port 3000)  

**Test:**  
- http://localhost:3000/ → "Server is running ✅"  
- http://localhost:3000/index.html → Portfolio site  
- All static assets served.
